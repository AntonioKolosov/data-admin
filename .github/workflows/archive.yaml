name: Zip repository and put on ???
on: [workflow_dispatch]

jobs:
  zip-n-push:
    name: Zip and Push
    runs-on: ubuntu-latest
    steps:
      - name: Zip Folder
        run: |
          cd ../ # I added this
          rm -rf {{ github.repository }}.zip
          zip -r {{ github.repository }}.zip {{ github.repository }} -x ".git/*" ".github/*"
